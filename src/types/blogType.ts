type BlogType = {
	title: string;
	authors: string;
	description: string;
	tags: string[];
	publishedAt: string;
	createdAt: string;
	updatedAt: string;
	content: string;
};

export default BlogType;
